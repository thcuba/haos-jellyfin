name: Docker Image Smoke Test
on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  smoke-test:
    name: Build & smoke test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./haos-jellyfin
          load: true
          tags: haos-jellyfin:test

      - name: Inspect image metadata
        run: |
          echo "=== METADATI IMMAGINE ==="
          docker image inspect haos-jellyfin:test --format '{{.Size}}' | awk '{printf "Dimensione immagine: %.2f MB\n", $1/1024/1024}'
          echo "Architettura: $(docker inspect haos-jellyfin:test --format '{{.Os}}/{{.Architecture}}')"
          echo "Entrypoint: $(docker inspect haos-jellyfin:test --format '{{json .Config.Entrypoint}}')"
          echo "Cmd: $(docker inspect haos-jellyfin:test --format '{{json .Config.Cmd}}')"
          echo "Exposed ports: $(docker inspect haos-jellyfin:test --format '{{json .Config.ExposedPorts}}')"
          echo "Env count: $(docker inspect haos-jellyfin:test --format '{{len .Config.Env}}')"

      - name: Verify image structure
        run: |
          echo "=== VERIFICA STRUTTURA ==="
          # Ogni verifica fallisce il job (set -e) se il binario è assente,
          # così lo smoke test è realmente bloccante.
          set -e

          docker run --rm --entrypoint ls haos-jellyfin:test /jellyfin/jellyfin > /dev/null 2>&1 && \
            echo "✅ Jellyfin binario presente" || { echo "❌ Jellyfin binario assente"; exit 1; }

          docker run --rm --entrypoint ls haos-jellyfin:test /usr/lib/jellyfin-ffmpeg/ffmpeg > /dev/null 2>&1 && \
            echo "✅ FFmpeg presente" || { echo "❌ FFmpeg assente"; exit 1; }

          docker run --rm --entrypoint sh haos-jellyfin:test -c "jq --version > /dev/null 2>&1" && \
            echo "✅ jq installato" || { echo "❌ jq assente"; exit 1; }

          docker run --rm --entrypoint sh haos-jellyfin:test -c "curl --version > /dev/null 2>&1" && \
            echo "✅ curl installato" || { echo "❌ curl assente"; exit 1; }

      - name: Verify layer count and size
        run: |
          echo "=== STRATI DELL'IMMAGINE ==="
          docker history haos-jellyfin:test --format "  {{.CreatedSince}}: {{.Size}} - {{.CreatedBy}}" | head -10
          echo "Layers: $(docker image inspect haos-jellyfin:test --format '{{len .RootFS.Layers}}')"

      - name: Cleanup
        if: always()
        run: |
          docker rmi haos-jellyfin:test 2>/dev/null || true